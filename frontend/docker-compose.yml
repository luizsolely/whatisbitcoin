services:

  # ── Frontend Angular ──────────────────────────────
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: whatisbitcoin-frontend
    ports:
      - "4200:80"
    restart: unless-stopped

    # Quando o backend Spring for adicionado, descomente:
    # depends_on:
    #   - backend
    # networks:
    #   - btc-network

  # ── Backend Spring Boot (futuro) ──────────────────
  # backend:
  #   build:
  #     context: ../backend
  #     dockerfile: Dockerfile
  #   container_name: whatisbitcoin-backend
  #   ports:
  #     - "8080:8080"
  #   environment:
  #     - SPRING_PROFILES_ACTIVE=docker
  #   restart: unless-stopped
  #   networks:
  #     - btc-network

# networks:
#   btc-network:
#     driver: bridge
